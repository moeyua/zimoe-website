---
import Layout from '../layouts/Layout.astro'
interface Props {
  title: string
}
const { title } = Astro.props
---

<Layout title={title} class="overflow-scroll scrollbar-hide">
  <div class="grid grid-cols-2 gap-6">
    <slot />
  </div>
</Layout>
